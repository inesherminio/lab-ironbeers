{{!-- Great job displaying the info and using hbs helpers correctly --}}
{{!-- However, try to use partials. They are basically a piece of code that can be reused --}}
{{!-- Given that you need almost the same info on beers.hbs and random-beer.hbs, it makes sense to have a partial --}}

{{#each randomObj}}

  <h2>{{this.name}}</h2>
  <h3>{{this.tagline}}</h3>
  <p>{{this.description}}</p>
  <img src='{{this.image_url}}' />

  <h4>Food Pairing</h4>
  <ul>
    {{#each this.food_pairing}}
      <li> {{this}}</li>
    {{/each}}
  </ul>
  <p><b>Brewer's Tips:</b>{{this.brewers_tips}}</p>
{{/each}} 

{{! Displaying info with partials }}

{{!-- {{#each randomObj}}

  {{> beerPartial }}

  <h4>Food Pairing</h4>
  <ul>
    {{#each this.food_pairing}}
      <li> {{this}}</li>
    {{/each}}
  </ul>
  <p><b>Brewer's Tips:</b>{{this.brewers_tips}}</p>
{{/each}}  --}}